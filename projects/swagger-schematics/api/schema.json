{
  "$schema": "http://json-schema.org/schema",
  "$id": "SwaggerApiSchema",
  "title": "Swagger Api Schema",
  "properties": {
    "path": {
      "type": "string",
      "description": "The path at which to create the files, relative to the current workspace.",
      "visible": false
    },
    "baseApiServicesPath": {
      "type": "string",
      "description": "Path for base API files (if not specified, the path property will be used)."
    },
    "swaggerSchemaUrl": {
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "apiPathKey": {
      "type": "string"
    },
    "apiServiceTemplatePath": {
      "type": "string"
    },
    "apiCrudServiceTemplatePath": {
      "type": "string"
    },
    "framework": {
      "type": "string",
      "description": "Target framework for generated API code.",
      "default": "angular",
      "enum": ["angular", "react-rtk"],
      "x-prompt": {
        "message": "Which framework would you like to generate API code for?",
        "type": "list",
        "items": [
          { "value": "angular", "label": "Angular (HttpClient services)" },
          { "value": "react-rtk", "label": "React RTK Query (API slices)" }
        ]
      }
    },
    "rtkBaseApiPath": {
      "type": "string",
      "description": "Import path for the RTK base API (e.g., '@/store/api/baseApi'). Required for RTK framework."
    },
    "scopeEndpointsWithTags": {
      "type": "boolean",
      "description": "Whether to scope endpoint names with tags for RTK Query. When true, generates names like 'postsGetAll' instead of 'getAll'. Recommended for APIs with multiple controllers to avoid naming collisions.",
      "default": false
    }
  },
  "required": []
}
