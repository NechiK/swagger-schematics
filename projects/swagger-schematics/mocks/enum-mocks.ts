export const CLAIM_STATUSES_ENUM_CONTENT: string = `export enum TClaimStatuses {
  1 = 1,
  2 = 2,
  3 = 3,
  99 = 99
}
`;

export const CLAIM_TYPE_ENUM_CONTENT: string = `export enum TClaimType {
  MS = 1,
  PP = 2
}
`;

export const CLAIM_TYPE_ENUM_NO_NAMES_CONTENT: string = `export enum TClaimType {
  1 = 1,
  1 = 2
}
`;
