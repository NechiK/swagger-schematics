{
  "$schema": "http://json-schema.org/schema",
  "$id": "SwaggerApiSchema",
  "title": "Swagger Api Schema",
  "properties": {
    "path": {
      "type": "string",
      "description": "The path at which to create the files, relative to the current workspace.",
      "visible": false
    },
    "baseApiPath": {
      "type": "string",
      "description": "Path for base API file. For Angular: defaults to path + '/_api-base.service.ts'. For RTK: defaults to 'th-common/store/api-base.ts'."
    },
    "swaggerSchemaUrl": {
      "type": "string",
      "description": "Source of the OpenAPI/Swagger schema: an http(s) URL, or a path to a local JSON file (absolute, relative to the project root, or a file:// URL).",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "apiPathKey": {
      "type": "string"
    },
    "apiServiceTemplatePath": {
      "type": "string"
    },
    "baseApiTemplatePath": {
      "type": "string",
      "description": "Custom template path for base API generation."
    },
    "framework": {
      "type": "string",
      "description": "Target framework for generated API code: 'angular' or 'react-rtk'.",
      "enum": ["angular", "react-rtk"]
    },
    "scopeEndpointsWithTags": {
      "type": "boolean",
      "description": "Whether to scope endpoint names with tags for RTK Query. When true, generates names like 'postsGetAll' instead of 'getAll'. Recommended for APIs with multiple controllers to avoid naming collisions.",
      "default": false
    },
    "typeMapping": {
      "type": "object",
      "description": "Map custom backend types to TypeScript primitives. Keys are schema names, values are TypeScript types (e.g., { 'SuperDuperInt32': 'number', 'CustomGuid': 'string' }).",
      "additionalProperties": {
        "type": "string"
      }
    },
    "templateHelpersPath": {
      "type": "string",
      "description": "Path to a JavaScript file exporting custom helper functions for use in templates. The exported functions and values will be available in the template context."
    },
    "eslintFix": {
      "type": "boolean",
      "description": "Run the host project's ESLint with autofix on generated files, using the project's own configuration (import sorting, commas, quotes, etc.). Never blocks generation: failures are reported as warnings and files keep their generated content.",
      "default": false
    }
  },
  "required": []
}
